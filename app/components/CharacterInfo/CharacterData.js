import React, { Component } from 'react';

export default class CharacterData extends Component {
    render() {
        return (
            <div>
                character data
            </div>
        );
    }
}