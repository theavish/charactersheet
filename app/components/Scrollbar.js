import React, { Component } from 'react';

export default class Scrollbar extends Component {
    render() {
        return (
            <div>
                scrollbar
            </div>
        );
    }
}